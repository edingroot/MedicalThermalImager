<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <!-- OpenCV -->
    <declare-styleable name = "CameraBridgeViewBase" >
        <attr name="show_fps" format="boolean"/>
        <attr name="camera_id" format="integer" >
            <enum name="any" value="-1" />
            <enum name="back" value="99" />
            <enum name="front" value="98" />
        </attr>
    </declare-styleable>

    <!-- .view.ScrollViewWithMaxHeight -->
    <declare-styleable name="ScrollViewWithMaxHeight">
        <attr name="maxHeight" format="dimension" />
    </declare-styleable>

    <!-- .view.ThermalSpotView -->
    <declare-styleable name="ThermalSpotView">
        <attr name="spotId" format="integer"/>
        <attr name="showId" format="boolean"/>
    </declare-styleable>

    <!-- .view.SpotsControlView -->
    <declare-styleable name="SpotsControlView">
        <attr name="enabled" format="boolean"/>
        <attr name="toggleVisibility" format="boolean"/>
    </declare-styleable>

</resources>
